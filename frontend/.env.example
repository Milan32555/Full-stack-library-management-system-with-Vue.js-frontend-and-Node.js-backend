# Desarrollo local
VITE_API_URL=http://localhost:3000/api

# Producción (Vercel Services: frontend y backend viven bajo el mismo dominio,
# asi que el path relativo alcanza. Configurar como variable de entorno del
# servicio frontend en Vercel, NO en este archivo)
# VITE_API_URL=/api

